# CIVION Mail

Thunderbird extension for local mail analysis, classification, document extraction, and integration with the CIVION personal administration system.

## Recovered release: v0.6.9

The authoritative recovered artifact is:

`releases/v0.6.9/CIVION_MAIL_v0_6_9.xpi`

SHA-256:

`e4e82d5cf7dc0734cd80aee272d0d9e569c26a86b7fc80ff38dec939d50a7841`

The XPI was recovered directly from the owner's active Thunderbird installation. Its manifest reports `CIVION Mail` version `0.6.9`, and the checksum matches the historical v0.6.9 verification record.

An XPI is a ZIP archive and contains the complete 32-file extension source tree. The expanded repository root is currently only partially recovered; files explicitly marked `RECOVERY STUB` must not be treated as canonical source. The verified XPI remains the source-of-truth recovery artifact until the complete tree is expanded and verified byte-for-byte.

See `RECOVERY_PROVENANCE.md` for provenance details.
